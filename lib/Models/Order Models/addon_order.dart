
/// An object that represents an addon when constructing an order in the GUI
class addon_order
{
  String name;
  int amount;
  double price;
  addon_order({required this.name, required this.price, required this.amount});
}